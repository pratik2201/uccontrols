//console.log('~~~:['+ __dirname +']');
import ucb from "ucbuilder"; 
ucb.registar({
    srcDir: __dirname,
    outDir: __dirname,
    /*html: __dirname,
    style: __dirname,
    perameters: __dirname,
    designer:__dirname,
    designerSrc:__dirname,
    code: __dirname,
    codeSrc: __dirname,*/
});