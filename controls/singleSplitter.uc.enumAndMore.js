const { commonEvent } = require("@ucbuilder:/global/commonEvent");

const spliterType = Object.freeze({
    NOT_DEFINED: "n",
    VERTICAL: "v",
    HORIZONTAL: "h",
});

module.exports = {
    spliterType,   
};